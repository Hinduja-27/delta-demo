pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                echo 'Jenkins build the code'
            }
        }
         stage('Test') {
            steps {
                echo 'Jenkins Test the code'
            }
        }
         stage('Deploy') {
            steps {
                echo 'Jenkins Deploy the code'
            }
        }
    }
}
